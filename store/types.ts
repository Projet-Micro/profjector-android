export type ProfessorState = {
    firstName: string;
    lastName: string;
    NIC: string;
    email: string;
    status: string;
}
export type ProjectorsState = {
    projId: number;
    brand: string;
    serialNumber: string;
    nbrCables: number;
    comment: string;
}